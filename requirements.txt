# LLM calls use stdlib urllib on purpose (no client SDK) — provider-agnostic
# OpenAI-compatible chain, easier to audit, nothing to pin.
langgraph>=1.0
# Distribution names differ from import paths: these two are imported as
# `langgraph.checkpoint.postgres` / `.sqlite` in src/checkpointing.py.
langgraph-checkpoint-postgres>=3.0
langgraph-checkpoint-sqlite>=3.0
psycopg[binary]>=3.2
psycopg-pool>=3.2
pydantic>=2.0
jinja2>=3.1
fastapi>=0.115
# Not imported — it's the process entrypoint: Dockerfile CMD runs
# `uvicorn src.app:app`.
uvicorn>=0.30
prometheus-client>=0.20
python-dotenv>=1.0
